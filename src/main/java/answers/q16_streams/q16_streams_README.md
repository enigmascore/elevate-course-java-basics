# Exercise q16_streams — streams

Implement both methods as stream pipelines: `stream()` → `filter` → `map` → `sorted` → `collect` for the first; `filter` → `count` for the second. No loops.

Run the test:

```bash
make test-one TEST=Q16StreamsTest
```
