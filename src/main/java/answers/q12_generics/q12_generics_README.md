# Exercise q12_generics — generics: Box<T> and a generic method

Implement the classic `Box<T>` (store / get) and the generic method `first`. The test uses the SAME code with `String` and `Integer` — that reuse with compile-time safety is what generics buy.

Run the test:

```bash
make test-one TEST=Q12GenericsTest
```
