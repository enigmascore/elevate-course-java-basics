package answers.q17_threads;

import java.util.List;

/**
 * Exercise q17 - a thread pool doing real work: Executors, ExecutorService,
 * Future.get.
 */
public class PoolWork {

    /**
     * Square every number USING A THREAD POOL: create a fixed pool ( e.g.
     * 4 threads ) with Executors, submit one task per number, collect each
     * result with Future.get() IN THE INPUT ORDER, and shut the pool down.
     */
    public List<Integer> squareAll( List<Integer> numbers ) throws Exception {
        throw new UnsupportedOperationException( "implement me" );
    }
}
