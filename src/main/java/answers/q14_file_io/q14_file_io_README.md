# Exercise q14_file_io — reading files

Implement both methods with `Files.readAllLines`. A line of only spaces counts as blank — `isBlank()` is your friend.

Run the test:

```bash
make test-one TEST=Q14FileIoTest
```
