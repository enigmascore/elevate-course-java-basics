# Exercise q06_arrays — arrays

Implement `sum`, `largest` and `reversed`. `reversed` must return a NEW array and leave the input untouched — the test checks both.

Run the test:

```bash
make test-one TEST=Q06ArraysTest
```
