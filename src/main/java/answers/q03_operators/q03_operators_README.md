# Exercise q03_operators — operators and integer division

Implement the four methods in `Arithmetic`. Mind the trap the course teaches: `7 / 2` is `3` for ints — `precisely` needs a cast before dividing.

Run the test:

```bash
make test-one TEST=Q03OperatorsTest
```
