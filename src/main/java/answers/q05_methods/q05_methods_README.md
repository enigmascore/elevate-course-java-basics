# Exercise q05_methods — methods, overloading, static vs instance

Implement the four methods. `add` is `static` — the test calls it on the CLASS; the two `describe` methods share a name and differ only by parameter type (overloading).

Run the test:

```bash
make test-one TEST=Q05MethodsTest
```
