# Exercise q07_collections — ArrayList and HashMap

Implement the three methods with an `ArrayList` and a `HashMap`. `countOf` wants the map method that takes a default for missing keys.

Run the test:

```bash
make test-one TEST=Q07CollectionsTest
```
