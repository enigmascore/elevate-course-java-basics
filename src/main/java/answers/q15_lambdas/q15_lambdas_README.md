# Exercise q15_lambdas — lambdas via Comparator

Implement `sortByLength` (sort the list with a comparator lambda — `Comparator.comparingInt` and a method reference make it one line) and `byLastLetter` (return a comparator lambda).

Run the test:

```bash
make test-one TEST=Q15LambdasTest
```
