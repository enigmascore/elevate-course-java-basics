# Exercise q01_variables — variables, primitives and casting

Implement the three methods in `VariableBasics`: widen an `int` to a `long`, truncate a `double` to an `int` with a cast, and return the largest possible `int` (the JDK provides a constant for it).

Run the test:

```bash
make test-one TEST=Q01VariablesTest
```
