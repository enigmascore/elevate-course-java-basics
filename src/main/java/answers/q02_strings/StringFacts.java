package answers.q02_strings;

/**
 * Exercise q02 - Strings: .equals vs ==, immutability in practice.
 */
public class StringFacts {

    /** True when the two strings have the same CONTENT. */
    public boolean sameContent( String a, String b ) {
        throw new UnsupportedOperationException( "implement me" );
    }

    /** True only when the two references point at the SAME object. */
    public boolean sameObject( String a, String b ) {
        throw new UnsupportedOperationException( "implement me" );
    }

    /**
     * Return the given word upper-cased with an exclamation mark appended.
     * The original string must be left untouched ( strings always are ).
     */
    public String shout( String word ) {
        throw new UnsupportedOperationException( "implement me" );
    }
}
