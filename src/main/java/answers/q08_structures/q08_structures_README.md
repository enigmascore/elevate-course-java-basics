# Exercise q08_structures — choosing a data structure

Each method names a BEHAVIOUR — FIFO, both-ends, insertion order, sorted keys, no duplicates. Pick the structure from the course's which-one-when table that delivers it (`ArrayDeque`, `LinkedHashMap`, `TreeMap`, `HashSet`).

Run the test:

```bash
make test-one TEST=Q08StructuresTest
```
