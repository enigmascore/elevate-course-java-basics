# Exercise q10_inheritance — inheritance and @Override

Implement `sound()` in `Dog` and `Cat`, and `describe()` ONCE in the abstract base — it must use `getName()` and `sound()`, which is the whole point of putting it there.

Run the test:

```bash
make test-one TEST=Q10InheritanceTest
```
