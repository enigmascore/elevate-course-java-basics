# Exercise q04_control_flow — if / switch / loops

Implement `grade` (if / else if / else), `dayName` (a switch is the natural fit) and `sumUpTo` (any loop you like).

Run the test:

```bash
make test-one TEST=Q04ControlFlowTest
```
