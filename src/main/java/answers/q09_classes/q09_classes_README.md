# Exercise q09_classes — classes, constructors, encapsulation

Complete `BankAccount`: the getters, a guarded `deposit` (refuse zero/negative with `IllegalArgumentException`) and `toString`. The fields stay private — that IS the exercise.

Run the test:

```bash
make test-one TEST=Q09ClassesTest
```
