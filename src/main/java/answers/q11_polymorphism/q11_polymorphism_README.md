# Exercise q11_polymorphism — polymorphism

Implement the three `area()` methods and `totalArea`, which iterates a `List<Shape>` calling the SAME method on each element — the run-time dispatch the course's polymorphism page teaches.

Run the test:

```bash
make test-one TEST=Q11PolymorphismTest
```
